<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ohseat.ohseatback.repository.UserRepository">

    <!-- 회원가입 -->
    <insert id="joinUser" parameterType="com.ohseat.ohseatback.domain.User">
        INSERT INTO users (email, name, nickname, password, phone_number)
        VALUES (#{email}, #{name}, #{nickname}, #{password}, #{phoneNumber})
    </insert>

    <!--  이메일 중복 확인  -->
    <select id="existsByEmail" parameterType="string" resultType="int">
        SELECT COUNT(*) FROM users WHERE email = #{email}
    </select>

    <!--  닉네임 중복 확인  -->
    <select id="existsByNickname" parameterType="string" resultType="int">
        SELECT COUNT(*) FROM users WHERE nickname = #{nickname}
    </select>

    <!--  로그인  -->
    <select id="findByEmail" parameterType="string" resultType="com.ohseat.ohseatback.domain.User">
        SELECT user_id, email, nickname, password
        FROM users
        WHERE email = #{email}
    </select>

    <!-- 마이페이지 조회 -->
    <select id="selectUserById" parameterType="int" resultType="com.ohseat.ohseatback.domain.User">
        SELECT user_id, name, email, nickname, password, phone_number, created_at
        FROM users
        WHERE user_id = #{userId}
    </select>

    <!--  마이페이지 수정  -->
    <update id="updateUserById" parameterType="com.ohseat.ohseatback.domain.User">
        UPDATE users
        SET nickname = #{nickname}, password = #{password}, phone_number = #{phoneNumber}
        WHERE user_id = #{userId}
    </update>

    <!--  마이페이지 삭제  -->
    <delete id="deleteUserById" parameterType="int">
        DELETE FROM users
        WHERE user_id = #{userId}
    </delete>

</mapper>
